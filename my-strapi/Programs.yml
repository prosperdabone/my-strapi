info:
  name: Programs
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:1337/api/programs?populate=image
  params:
    - name: populate
      value: image
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
